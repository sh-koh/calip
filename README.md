# 🌐 Calip - WIP 
Calip is a CLI tool to deal with networking stuff.

# 📔 Features
- Convert each byte of an IPv4 into binary
- ... that's it for now ...

# 🔨 Building
You can build it using `go`:
```bash
go build .
```

Or you can build it with `nix`:
```bash
nix build .#
```

# 🗿 Motivation
I'm practicing my programming skills for studies so i made this.
